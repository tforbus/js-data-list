/**
 * Concatenates two lists together.
 *
 * Example:
 * concat([x1, ..., xm], [y1, ..., yn]) == [x1, ..., xm, y1, ...yn]
 * concat('Foo', 'Bar') == 'FooBar'
 */
export default function concat(as, bs) {
    return as.concat(bs);
}
